Make sure you install node js and python.

In Python installation make sure to add python and pip to system variables.

To start project run startScript.bat, all apps should run in different terminals.